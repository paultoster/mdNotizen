## 



## 1. Setup



```bash
# Befehl zum Installieren der Pakete
sudo apt-get install cryptsetup 

# da nicht funktioniert habe ic update gemcht
sudo apt-get update

# benötigte Kernelmodul mittels Befehls laden
sudo modprobe dm-crypt 


```
